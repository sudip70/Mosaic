// Curated Lucide icon set — minimalist outline icons used across the app's UI
// chrome. Import from here so the set stays consistent and swappable.
export {
  Aperture,     // Today tab
  Grid2x2,      // Grid tab
  Users,        // Friends tab
  Settings,     // Settings tab + camera settings
  Camera,       // capture CTA
  X,            // close
  ChevronLeft,  // back
  Ellipsis,     // menu (···)
  Share2,       // share
  Download,     // download
  Trash2,       // delete
  Plus,         // add
  Zap,          // flash
  RotateCcw,    // flip camera
  ArrowRight,   // forward CTA
  Sparkles,     // magic link
} from 'lucide-react-native';

export type { LucideIcon } from 'lucide-react-native';

// Default stroke for the minimalist look.
export const ICON_STROKE = 2;
